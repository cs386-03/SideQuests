function totalPoints( points, pointTotal ) {
    points = Number(points);
    pointTotal = points + pointTotal;

    console.log("total points ran");
    return pointTotal;
}

module.exports = totalPoints;